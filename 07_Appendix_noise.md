Appendix D — Noise Extension

Add stochastic perturbation:

\phi(t) \to \phi(t) + \eta(t), \quad \eta \sim \mathcal{N}(0, \sigma^2)

Correction becomes noisy signal detector. Resilience holds under low noise; fails at high \sigma.

⸻
